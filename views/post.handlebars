{{#if loggedIn}}{{else}}
  <h2 class='signin-prompt'><a href='/api/login'>Sign In</a>
    Or
    <a href='/api/login'> Sign Up</a>
    To Comment Or Post</h2>

{{/if}}

<div class='card'>
  <div class='car-header'>
    <h1 class='single-post-text'>{{post.title}}</h1>
    <p class='single-post-text'>Posted by
      {{post.user.user_name}}
      on
      {{format_date post.created_at}}</p>
  </div>
  <div class='card-text'>
    <p class='single-post-text'>
      {{post.post}}
    </p>
  </div>
</div>
{{#if loggedIn}}
  <div class='comment-input-section'>
    <input class='comment-input' type='text' />
    <button class='comment-button'>Comment</button>
  </div>

{{/if}}
  <h3 style='text-decoration: underline;'>Comments</h3>
  {{#each post.comments as |all|}}
    <div class='comments-section'>
      <p style='font-weight: 800;'>Posted by {{all.user.user_name}} on  {{format_date created_at}}</p>

      <p class='comment'>
        {{all.comment}}

      </p>
      <button data-post-id="{{all.id}}"  data-user-id="{{all.user_id}}" class="delete-comment-btn">Delete</button>
      <p class="err"></p>
    </div>
  {{/each}}
<script src='../js/comment.js'></script>
<script src="../js/deleteComment.js"></script>

